apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homepage
  namespace: toolbox
  labels:
    app: homepage
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`dash.loutik.fr`)
      kind: Rule
      services:
        - name: homepage
          port: 3000