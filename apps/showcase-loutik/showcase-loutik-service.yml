apiVersion: v1
kind: Service
metadata:
  name: showcase-loutik
  namespace: website
  labels:
    app.kubernetes.io/name: showcase-loutik
spec:
  selector:
    app.kubernetes.io/name: showcase-loutik
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080